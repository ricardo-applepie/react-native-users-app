import React from 'react';
import {View, Image, StyleSheet, Text} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
  },
  Image: {
    height: 300,
    width: '100%',
  },
});

function PostDescription() {
  return (
    <View>
      <Text> I Feel so so happy now </Text>
    </View>
  );
}

export default PostDescription;
